# ==============================================================================
# Variáveis de Ambiente - PRODUÇÃO
# ==============================================================================
# ATENÇÃO: Este é um arquivo de exemplo. Nunca commite o .env.production real!
# Copie este arquivo para .env.production e configure com valores reais.
# ==============================================================================

# ------------------------------------------------------------------------------
# Database (PostgreSQL)
# ------------------------------------------------------------------------------
DB_USER=checkin_user
DB_PASSWORD=TROCAR_SENHA_FORTE_AQUI_min16caracteres!@#
DB_NAME=checkin_db

# Connection string completa
DATABASE_URL=postgresql://checkin_user:TROCAR_SENHA_FORTE_AQUI@postgres:5432/checkin_db?schema=public

# ------------------------------------------------------------------------------
# NextAuth.js
# ------------------------------------------------------------------------------
# URL pública da aplicação (com seu domínio)
NEXTAUTH_URL=https://checkin.seudominio.com

# Secret para JWT (GERAR COM: openssl rand -base64 32)
NEXTAUTH_SECRET=TROCAR_POR_SECRET_GERADO_COM_OPENSSL_32_CARACTERES_MINIMO

# ------------------------------------------------------------------------------
# App Configuration
# ------------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://checkin.seudominio.com
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# ------------------------------------------------------------------------------
# Email / SMTP (Opcional - para envio de QR Codes)
# ------------------------------------------------------------------------------
# Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=seu-email@gmail.com
SMTP_PASS=sua-app-password-do-gmail
SMTP_FROM=noreply@checkin.seudominio.com

# OU Outlook
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USER=seu-email@outlook.com
# SMTP_PASS=sua-senha

# OU SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.sua-api-key-aqui

# ------------------------------------------------------------------------------
# Segurança Adicional (Opcional)
# ------------------------------------------------------------------------------
# Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=900000

# CORS Origins permitidos (separar por vírgula)
ALLOWED_ORIGINS=https://checkin.seudominio.com

# ------------------------------------------------------------------------------
# Backup (Usado pelo serviço de backup)
# ------------------------------------------------------------------------------
BACKUP_SCHEDULE=0 3 * * *
BACKUP_RETENTION_DAYS=7

# ------------------------------------------------------------------------------
# Monitoramento (Opcional)
# ------------------------------------------------------------------------------
# Sentry
# SENTRY_DSN=https://xxx@xxx.ingest.sentry.io/xxx

# New Relic
# NEW_RELIC_LICENSE_KEY=xxx
# NEW_RELIC_APP_NAME=Check-IN System

# ==============================================================================
# INSTRUÇÕES
# ==============================================================================
# 
# 1. Copie este arquivo:
#    cp .env.production.example .env.production
#
# 2. Gere um secret forte:
#    openssl rand -base64 32
#
# 3. Edite .env.production com seus valores reais
#
# 4. NUNCA commite .env.production no Git!
#
# ==============================================================================
